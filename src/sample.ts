const { assert } = require('chai');

describe('Sample test suite', function() {
    beforeEach(function() {
        
    });

    afterEach(function() {
        
    });

    it('behaves this way when user does that', function(){
        // arrange
        // (initial conditions and state)
        
        // act
        // (execute command)
    
        // assert
        // (verify outcome)
    
    });
});